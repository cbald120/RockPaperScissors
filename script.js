

let guessSubmit = document.querySelector('.guessSubmit');
let guessField = document.querySelector('.guessField');

let arr = ["Rock", "Paper", "Scissors"]; 
let randomAction = Math.floor(Math.random() * arr.length);

function playRound(playerSelection, computerSelection) {
    if UserPick == "rock" && getComputerChoice == "scissors";
    return "You Win Mista White!"
  }


//function getComputerChoice() {
    //inside works so maybe cut from function? let arr = ["Rock", "Paper", "Scissors"]; let randomAction = Math.floor(Math.random() * arr.length);
}


